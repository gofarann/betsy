<h1><%= @title %></h1>
<div class="table-master">
<table class='table table-hover'>
  <tr>
    <td class="table-headers">
      Name
    </td>
    <td>
      <%= @product.name %>
    </td>
  </tr>
  <tr>
    <td class="table-headers">
      Description
    </td>
    <td>
      <%= @product.description%>
    </td>
  </tr>
  <tr>
    <td class="table-headers">
      Price
    </td>
    <td>
      <%= @product.price %>
    </td>
  </tr>
  <tr>
    <td class="table-headers">
      Photo
    </td>
    <td>
      <%= link_to "See image", @product.photo_url, data: { confirm: "This will take you away from the site. Are you sure?" } %>
    </td>
  </tr>
  <tr>
    <td class="table-headers">
      Stock
    </td>
    <td>
      <%= @product.stock%>
    </td>
  </tr>
  <tr>
    <td class="table-headers">
      Category
    </td>
    <td>
      <% @product.categories.each do |category| %>
      <%= category.name %>
      <% end %>
    </td>
  </tr>
  <tr>
    <td class="table-headers">
      Average Rating
    </td>
    <td>
      <%= @product.avg_rating %>
    </td>
  </tr>
</table>
</div>


<%= button_to "Edit", edit_product_path(@product), method: :get, class: "btn btn-default" %>Edit button needs to filter for logged in only
<%= button_to "Delete", product_path(@product.id), method: :delete, class: "btn btn-default" %>
<%= button_to "All Products", products_path, method: :get, class: "btn btn-default" %>

Need add review button to go to review form and filter so owner of product cannot see
Need buy button to go to that filters so owner of product cannot see
Need add category button
