<% collection.products.to_ary.in_groups_of(3, false) do |group| %>
  <div class="row">
    <% group.each do |product| %>
      <div class="col-sm-4">
        <%= link_to "#{product.name}", product_path(product) %>
      </div>
    <% end %>
  </div>
<% end %>